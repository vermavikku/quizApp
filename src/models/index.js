const users = require("./users.models");
const topics = require("./topics.models");
const questions = require("./questions.models");
const userTopics = require("./users_topics.models");
const results = require("./results.models");

module.exports = {
    users,
    topics,
    questions,
    userTopics,
    results
}