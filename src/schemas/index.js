const users = require("./users.schemas");
const topics = require("./topics.schemas");
const questions = require("./questions.schemas");
const userTopics = require("./users_topics.schemas");
const results = require("./results.schemas");
module.exports = {
    users,
    topics,
    questions,
    userTopics,
    results
}